describe( 'Initialization Test', function() {
	it( "Checks that api.Init is defined.", function() {
		expect( BOLDGRID.SEO.Init ).toBeDefined();
	});
	it( "Checks that api.Init.load() is defined.", function() {
		expect( BOLDGRID.SEO.Init.load ).toBeDefined();
	});
});
